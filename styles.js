import { StatusBar } from 'react-native';

export default {
  container: {
    backgroundColor: "#FFF"
  },
  mb10: {
    marginBottom: 10
  },
  header: {
    marginTop: StatusBar.currentHeight,
  }
};
